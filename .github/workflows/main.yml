name: gradle
on:
  push:
    branches: [ main ] 
 
jobs:
  elastic:
    runs-on: ubuntu:latest
    steps:

      - name: Gradle version number  
        uses: Vishwastp89/version@main
        with:
          filename: build.gradle
